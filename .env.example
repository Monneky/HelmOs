# =============================================================================
# HelmOs - Variables de entorno
# Copia este archivo a .env y ajusta los valores según tu entorno.
# =============================================================================

# Dominio principal donde se sirve HelmOs (ej: helmos.local, app.ejemplo.com)
HELMOS_DOMAIN=localhost

# Email del administrador (notificaciones, recuperación, etc.)
HELMOS_EMAIL=admin@example.com

# Directorio donde se almacenan datos persistentes (SQLite, uploads, etc.)
# En Docker suele ser un volumen montado, ej: /data
HELMOS_DATA_DIR=./data

# Puerto en el que escucha la API (por defecto 3001)
HELMOS_PORT=3001

# Secreto para firmar y verificar tokens JWT. Debe ser largo y aleatorio en producción.
JWT_SECRET=change-me-to-a-secure-random-string

# Email del usuario administrador inicial (login del dashboard)
ADMIN_EMAIL=admin@example.com
